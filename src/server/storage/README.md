# Storage (stub — Phase 4)

`storage-provider.interface.ts` is a real TypeScript interface. `minio-storage.provider.ts`
throws `"Not implemented until Phase 4"` on every method and is imported nowhere in
Phase 1-2. Do not wire this up until the Phase 4 `PlanGenerationEngine` needs it.
